name: miniapp_runtime_engine
description: Official MiniApp Runtime Engine with secure sandbox and native bridge
version: 1.0.0
homepage: https://github.com/your-org/miniapp-runtime-engine

environment:
  sdk: ^3.11.1
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^4.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  # This package supports all platforms
  # Add any assets if needed
